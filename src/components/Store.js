import React from 'react';
import Product from './Product';
import Navbar from './Navbar';

const Store = () => {
    return (
        <div>
            <Navbar/>
            <Product/>
        </div>
    );
};

export default Store;